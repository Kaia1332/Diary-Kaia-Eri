DROP TABLE IF EXISTS diary;
CREATE TABLE diary (
    id INT NOT NULL,
    date DATE NOT NULL,
    time TIME NOT NULL,
    category VARCHAR(30) NOT NULL,
    content TEXT NOT NULL,
    PRIMARY KEY (id)
);




















































































































